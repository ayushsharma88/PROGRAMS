#include<stdio.h>
#include<iostream>
#include <stdlib.h>
#include <string.h>


using namespace std;
void rotate (char *str)
{
    if(*(str))
    {
        rotate((str+1));
        cout<<*str;
    }
}
int main()
{
char str[20]="Ayush Sharma";
rotate(str);
cout<<endl;
return 0;
}

